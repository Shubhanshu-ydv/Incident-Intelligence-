# Dockerfile for Pathway RAG Pipeline
FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy Pathway application
COPY app.py .
COPY app.yaml .

# Expose port
EXPOSE 8081

# Run Pathway
CMD ["python", "app.py"]
